import React from "react";

const Header = () => {
    return (
        <div>Header</div>
    )
}

export default Header
// Header is exported as default, so it can be imported in other files without using curly braces.
